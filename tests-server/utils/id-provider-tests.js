let IdProvider = require('../../dist-server/utils/id-provider').IdProvider;
let expect = require('chai').expect;

describe('IdProvider', () => {
    describe('getNext()', () => {
        it('Should return sequential values.', () => {
            
        });
    });
});